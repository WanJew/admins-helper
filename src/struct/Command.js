class Command {
    constructor(options) {
        this.name = options.name;
        this.dir = dir
    }

    run() {
        console.log(`[ERR]: Функция на запуск команды не указана!`)
    }
}

module.exports = { Command };

// - Исправить - //